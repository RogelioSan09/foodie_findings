<!DOCTYPE html>
<html>

<head>
    <title>Search Results</title>
    <link rel="stylesheet" href="https://cdn.tailwindcss.com" />
</head>

<body class="bg-yellow-200 font-sans">

    <header class="bg-yellow-500 text-white py-4">
        <img src="./images/plate.png" alt="fork and knife line drawing" class="mx-auto w-48">
        <h1 class="text-3xl text-center font-bold mt-4">Search by Cuisine</h1>
        <form action="/search" method="GET" class="mt-4 flex justify-center">
            <input type="text" name="query" placeholder="Search..." class="border border-gray-400 rounded-l px-4 py-2 w-1/2">
            <button type="submit" class="bg-yellow-700 rounded-r text-white px-4 py-2">Search</button>
        </form>
    </header>

    <main class="flex flex-col md:flex-row">

        <section class="results flex-1 md:mr-4 mb-4 md:mb-0">
            <h2 class="text-2xl font-bold mb-4">Restaurants</h2>
            <ul>
                {{#each restaurants}}
                <li class="mb-2">{{this.name}}</li>
                <li class="mb-2">{{this.image_url}}</li>
                <li class="mb-2">{{this.url}}</li>
                <li class="mb-4">{{this.distance}}</li>
                {{/each}}
            </ul>
        </section>

        <section class="results flex-1 md:ml-4">
            <h2 class="text-2xl font-bold mb-4">Recipes</h2>
            <ul>
                {{#each recipes}}
                <li class="mb-2">{{this.title}}</li>
                <li class="mb-2">{{this.image}}</li>
                <li class="mb-2">{{this.sourceUrl}}</li>
                <li class="mb-4">{{this.readyInMinutes}}</li>
                {{/each}}
            </ul>
        </section>

    </main>

</body>

</html>
