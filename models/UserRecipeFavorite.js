const { Model, DataTypes } = require('sequelize');
const sequelize = require('../config/connection');

class UserRecipeFavorite extends Model {
  // id
  // userId
  // recipeId TODO: find this from the spoonacular api
}