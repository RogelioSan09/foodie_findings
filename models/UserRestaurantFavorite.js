const { Model, DataTypes } = require('sequelize');
const sequelize = require('../config/connection');

class UserRestaurantFavorite extends Model {
  // id
  // userId
  // restaurantId TODO: find this from the yelp api

  // TODO create a function to save a favorite restaurant
}